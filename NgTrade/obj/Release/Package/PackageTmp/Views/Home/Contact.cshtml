@model NgTrade.Models.ViewModel.ContactViewModel

@{
  ViewBag.Title = "Nigerian Stock Exchange Trading Online NSE Daily Price List - NgTradeOnline Contact Us";
}

<div class="content">
  <div class="container">
    <div class="page-header">
      <h2>Contact NgTradeOnline<sup style="font-size: 15px;">SM</sup></h2>
    </div>
    <div class="row-fluid">
      <!-- Start: CONTACT US FORM -->
      <div class="span4 offset1">
         @using (Html.BeginForm("Contact", "Home", new { ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-contact-us" }))
            {
          <div class="control-group">
            <div class="controls">
              @Html.TextBoxFor(m=> m.Name, new {@placeholder="Name"})
            </div>
          </div>
          <div class="control-group">
            <div class="controls">
              @Html.TextBoxFor(m=> m.Email, new {placeholder="Email"})
            </div>
          </div>
          <div class="control-group">
            <div class="controls">
              @Html.TextAreaFor(m=> m.Message, new {placeholder="Message"})
            </div>
          </div>
          <div class="control-group">
            <div class="controls">
              <input type="submit" class="btn btn-success btn-large" value="Send">
            </div>
          </div>
        }
      </div>
    </div>
  </div>
</div>
    <!-- End: MAIN CONTENT -->